// CREATE NEW CUSTOMER, PAGE "/customer/generate-qrcode"

export interface CreateNewCustomerTypes {
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  carName: string;
  carLicensePlate: string;
}
